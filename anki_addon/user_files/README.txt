MCP AnkiConnect writes bootstrap and server diagnostics without tunnel
credentials to server.log here.

The runtime directory contains the add-on's managed uv executable, Python
installation, and package caches. Files in this directory are preserved across
Anki restarts and add-on updates. They are local runtime data and are never
included in an add-on release archive.
